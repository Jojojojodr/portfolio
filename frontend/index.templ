package frontend

import (
    "github.com/gin-gonic/gin"
)

templ Index(c *gin.Context) {
    @Layout(c, "Home") {
        <div class="flex flex-col items-center justify-center">
            <h1 class="pb-3 text-2xl">Hello World!!</h1>
            <p>This site is under construction!!</p>
        </div>
    }
}